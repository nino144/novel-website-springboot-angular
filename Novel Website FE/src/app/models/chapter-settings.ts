export interface ChapterSettings {
  selectedFont: String;
  selectedFontSize: String;
  selectedLineHeight: String;
  selectedAlign: String;
  colors: String[];
  selectedColor: String; // Add this property if it's missing
  selectedBackgroundColor: String;
}